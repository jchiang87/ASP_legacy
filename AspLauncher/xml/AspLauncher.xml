<?xml version="1.0" encoding="UTF-8"?>
<pipeline
   xmlns="http://glast-ground.slac.stanford.edu/pipeline"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://glast-ground.slac.stanford.edu/pipeline http://glast-ground.slac.stanford.edu/Pipeline-II/schemas/2.0/pipeline.xsd">
<!-- $Header$
-->
<task name="AspLauncher" version="2.2" type="Data">
  <variables>
    <var name="BINDIR">rh9_gcc32</var>
    <var name="minimum_coverage">0.70</var>
    <var name="datacatalog_imp">datacatalog</var>
  </variables>
  <prerequisites>
    <prerequisite name="folder" type="string"/>
    <prerequisite name="nDownlink" type="integer"/>
    <prerequisite name="interval" type="integer"/>
    <prerequisite name="frequency" type="string"/>
    <prerequisite name="nMetStart" type="integer"/>
    <prerequisite name="nMetStop" type="integer"/>
    <prerequisite name="GRBOUTPUTDIR" type="string"/>
    <prerequisite name="DRPOUTPUTDIR" type="string"/>
    <prerequisite name="PGWAVEOUTPUTDIR" type="string"/>
    <prerequisite name="PIPELINESERVER" type="string"/>
    <prerequisite name="ASPLAUNCHERROOT" type="string"/>
  </prerequisites>
  <process name="catalogQueries">
    <script>
      <![CDATA[
        print "using datacatalog implementation: ", datacatalog_imp
        datacat = eval(datacatalog_imp)
        def query(outfile, path, TSTART, TSTOP):
            output = pipeline.createFile(outfile)
            opt1='(%(TSTART)i <= nMetStart && nMetStop <= %(TSTOP)i)'%locals()
            opt2='(nMetStart <= %(TSTART)i && %(TSTART)i <= nMetStop)'%locals()
            opt3='(nMetStart <= %(TSTOP)i && %(TSTOP)i <= nMetStop)' % locals()
            query = ('DataType == \"%s\" && (%s || %s || %s)' 
                     % (path, opt1, opt2, opt3))
            datasetList = datacat.getDatasets(folder+'/*', query)
            datasetList.writeToFile(output, 1)
            pipeline.writeFile(output)
        query("Ft1FileList", "FT1", nMetStart, nMetStop)
        query("Ft2FileList", "FT2", nMetStart, nMetStop)
    ]]></script>
  </process>
  <process name="launchStreams">
    <job executable="${ASPLAUNCHERROOT}/${BINDIR}/launchStreams.sh"
         batchOptions="-q short"/>
    <depends>
      <after process="catalogQueries" status="SUCCESS"/>
    </depends>
  </process>

  <process name="runInsertIntervals">
    <script>
      <![CDATA[
        datacat = eval(datacatalog_imp)
        query = 'DataType == \"FT1\" && nDownlink == %i' % nDownlink
        datasetList = datacat.getDataSets(logicalPath, 0, 0, "FT1", 0, query,
                                          (), ('nMetStart', 'nMetStop'), ())
        it = datasetList.iterator()
        #
        # There should only be one dataset returned, but loop over
        # all of the entries anyways.
        #
        while it.hasNext():
            dataset = it.next()
            nMetStart = dataset.getMetaValue('nMetStart')
            nMetStop = dataset.getMetaValue('nMetStop')
            pipeline.createSubstream("insertIntervalsTask", -1, 
                                     "nMetStart=%i, nMetStop=%i"
                                     % (nMetStart, nMetStop))
    ]]></script>
    <createsSubtasks>
      <subtask>insertIntervalsTask</subtask>
    </createSubtasks>
  </process>

  <task name="insertIntervalsTask" version="1.0" type="Data">
    <prerequisites>
      <prerequsite name="nMetStart" type="integer"/>
      <prerequsite name="nMetStop" type="integer"/>
    </prerequisites>
    <process name="insertInterval">
      <job executable="${ASPLAUNCHERROOT}/${BINDIR}/insertIntervals.sh"
           batchOptions="-q short"/>
    </process>
  </task> <!--insertIntervalsTask-->
</task><!--AspLauncher-->
</pipeline>
